node_modules/
.env
.env.local
.env.*.local
data/_store/
# real DB dumps contain member PII — never commit (the synthetic sample-chamberware.sql is fine)
woodlandhills*.sql
*-db.sql
*.dump
*.log
.DS_Store
Thumbs.db

# reference screenshots — do NOT deploy (would be public)
SCREENSHOTS/

# AGMS merchant credentials from Eduardo (screenshots contain the PRIVATE
# security key) — never commit; keys go in Render env vars only
agms/

# Member roster exports — PII, never commit
*.xlsx

# Source newsletter PDFs (95–410 MB each) — never commit; we ship rendered
# WebP page images under newsletters/assets/connect/ instead.
Biz Newsletter/

# local dev launcher (seeds a throwaway test admin) - never commit
local-dev.cjs
